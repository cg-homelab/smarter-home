export interface Home {
    id: string
    name: string
    address: string
    writeToken: string
}

export interface NewHome {
    name: string
    address: string
}

export interface UpdateHome {
    name: string
    address: string
}
