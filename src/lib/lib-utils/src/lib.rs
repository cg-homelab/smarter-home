/// Utility library for common functions and types used across the project.
pub mod crypto;
pub mod spatial_hash;

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(4, 4);
    }
}
